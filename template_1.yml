Resources:
  GlueJob:
    Type: AWS::Glue::Job
    Properties:
      Name: MyGlueJob  
      Role: !GetAtt GlueServiceRole.Arn
      Command:
        Name: glueetl
        ScriptLocation: s3://mycodepipelinebucket/script/glue_script.py?versionId=Q2Ri6tr6OZ38.wCZxfEhKdDSs5SqnE0p
      DefaultArguments:
        "--TempDir": "s3://mycodepipelinebucket/temp/"
      GlueVersion: "2.0"
      WorkerType: G.1X
      NumberOfWorkers: 2


  GlueServiceRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub "${AWS::StackName}-MyRoleForCloudFormation1"
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              Service: glue.amazonaws.com
            Action: sts:AssumeRole
      ManagedPolicyArns:
        - arn:aws:iam::aws:policy/AmazonS3FullAccess
        - arn:aws:iam::aws:policy/service-role/AWSGlueServiceRole
