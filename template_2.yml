AWSTemplateFormatVersion: '2010-09-09'
Description: 'CloudFormation template for creating an S3 bucket'

Resources:
  MyS3Bucket:
    Type: 'AWS::S3::Bucket'
    Properties:
      BucketName: 'mytestbucket20'  # Replace with your desired bucket name
      

Outputs:
  BucketName:
    Description: 'The name of the created S3 bucket'
    Value: !Ref S3Bucket
